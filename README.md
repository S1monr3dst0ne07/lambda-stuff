# Lambda Calculus Stuff 
This repo contains a basic lambda calculus interpreter `interpreter.py` in python,
and accompanying standard libraray `lib/` and programs `prg/`.

> [!Warning]
> Disclaimer: Interpreter is unbelievably inefficient and may be buggy.
